# Production Environment Variables Template
# Copy this file and fill in your actual values

# ============================================
# Backend Environment Variables (Railway)
# ============================================

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key-here

# MongoDB Atlas Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# Google Gemini API Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# JWT Authentication Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend URL for CORS (add your Vercel URL here)
FRONTEND_URL=https://your-app.vercel.app

# ============================================
# Frontend Environment Variables (Vercel)
# ============================================

# Backend API URL (set this in Vercel dashboard)
VITE_API_URL=https://your-backend-app.up.railway.app

# ============================================
# DEPLOYMENT INSTRUCTIONS
# ============================================

# 1. RAILWAY (Backend):
#    - Go to Railway dashboard → Your project → Variables
#    - Add all variables from "Backend Environment Variables" section above
#    - Railway will auto-restart after adding variables

# 2. RAILWAY - Add FRONTEND_URL:
#    - After deploying frontend to Vercel, get your Vercel URL
#    - Add to Railway: FRONTEND_URL = https://your-app.vercel.app
#    - This is needed for CORS to allow frontend requests

# 3. VERCEL (Frontend):
#    - Go to Vercel dashboard → Your project → Settings → Environment Variables
#    - Add: VITE_API_URL = https://your-backend-app.up.railway.app
#    - Redeploy frontend after adding

# 4. SECURITY NOTES:
#    - Generate JWT_SECRET_KEY with: openssl rand -hex 32
#    - Never commit actual .env files to git
#    - Use different keys for development and production
#    - MongoDB Atlas: Add Railway's IP to whitelist (or use 0.0.0.0/0)
#    - Supabase: Ensure RLS policies are enabled

# 5. MONGODB ATLAS IP WHITELIST:
#    - Option A (Testing): Add 0.0.0.0/0 (allows all IPs)
#    - Option B (Production): Add Railway's outbound IPs
#    - Location: MongoDB Atlas → Network Access → Add IP Address

# 6. CORS CONFIGURATION:
#    - CORS is automatically configured using FRONTEND_URL environment variable
#    - Make sure to set FRONTEND_URL in Railway after deploying to Vercel
